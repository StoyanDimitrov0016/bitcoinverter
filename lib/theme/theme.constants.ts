/** Colors for renderers that cannot resolve the application's CSS custom properties. */
export const STATIC_THEME_COLORS = {
  lightBackground: "#f5f0e6",
  darkBackground: "#17130f",
  darkForeground: "#f7f0e3",
  darkMuted: "#d7cbbb",
  accent: "#f59e0b",
  accentForeground: "#21170b",
} as const;
